Q1. Write a function fetchData(callback) that simulates fetching data with setTimeout. After 2 seconds, it should return "Data fetched!" using the callback.
Q2. Write a function addNumbers(a, b, callback) that takes two numbers and a callback, then returns the sum using the callback.
Q3. Create a promise isEven(num) that resolves if the number is even, rejects if odd.
Q4. Create a promise chain:
Fetch user → "User fetched"


Fetch posts → "Posts fetched"


Finally print "All data ready"
Q5. Rewrite Q4 using async/await.
Q6. Create a function delay(ms) that waits for ms milliseconds before returning "Done!" using async/await.


// //Q5.
//  function fetchUserr() {
//     return new Promise ((resolve) => {
//         console.log("Fetching user...");
//         setTimeout(() => resolve("User fetched"), 2000);
//     });
// }

// function fetchPostss() {
//     return new Promise ((resolve) => {
//         console.log("Fetching posts...");
//         setTimeout(() => resolve("Posts fetched"), 3000);
//     }); 
// }

// async function fetchDataAll() {
//     try {
//         const user= await fetchUserr();
//         console.log(user);

//         const posts= await fetchPostss();
//         console.log(posts);

//         console.log("All data fetched");

//     } 

//     catch (error) {
//         console.log("Error:", error);
//     }
// }

//  fetchDataAll();

//Q6.

function delay(ms) {
    return new Promise((resolve) => {
        setTimeout(() => resolve("done"), ms);
    });
}

async function runDelay() {
    console.log("Waiting...");
    const result = await delay(3000);
    console.log(result);
}

runDelay();